# 🌸 Interactive Animated Effects Guide / Guía de Efectos Animados Interactivos 🌸

## English Version

---

## Table of Contents
1. [Requirements](#requirements)
2. [Core Concepts](#core-concepts)
3. [Step-by-Step Tutorial](#step-by-step-tutorial)
4. [Code Examples](#code-examples)
5. [Tips & Best Practices](#tips--best-practices)

---

## Requirements

### Software & Libraries

| Requirement | Version | Purpose |
|-------------|---------|---------|
| Modern Web Browser | Chrome 80+, Firefox 75+, Safari 13+, Edge 80+ | CSS3 & ES6+ support |
| GSAP (GreenSock Animation Platform) | 3.12.5+ | JavaScript animation library |
| GSAP Draggable Plugin | 3.12.5+ | Drag-and-drop functionality |

### CDN Links
```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/Draggable.min.js"></script>
```

### No Build Tools Required
This project uses vanilla HTML, CSS, and JavaScript. No npm, webpack, or bundlers needed!

---

## Core Concepts

### 1. CSS Keyframe Animations
CSS animations allow elements to transition between styles smoothly.

**Key Properties:**
- `@keyframes` - Defines the animation sequence
- `animation` - Applies the keyframe animation
- `animation-duration` - How long the animation takes
- `animation-iteration-count` - Number of repetitions (`infinite` for loops)
- `animation-timing-function` - Easing (ease, linear, ease-in-out)

### 2. SVG (Scalable Vector Graphics)
SVGs are resolution-independent graphics defined in XML format.

**Advantages:**
- Scalable without quality loss
- Manipulable via JavaScript
- Styleable with CSS
- Small file sizes for simple graphics

### 3. GSAP (GreenSock Animation Platform)
GSAP is a powerful JavaScript animation library.

**Key Methods:**
- `gsap.to()` - Animate to specific values
- `gsap.from()` - Animate from specific values
- `gsap.to(element, {duration, property, ease})`
- Easing functions: `back.out`, `elastic.out`, `sine.inOut`, `power2.out`

### 4. GSAP Draggable
Enables drag-and-drop functionality with physics-based interactions.

**Key Features:**
- Inertia (momentum-based movement)
- Bounds constraints
- Callbacks (onDragStart, onDrag, onDragEnd)

---

## Step-by-Step Tutorial

### Step 1: HTML Structure

Create a container-based layout:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Animation Project</title>
    <link rel="stylesheet" href="styles.css">
    <!-- GSAP Libraries -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/Draggable.min.js"></script>
</head>
<body>
    <div class="container">
        <!-- Background elements -->
        <div class="background">
            <div class="sun"></div>
            <div class="cloud cloud-1"></div>
        </div>
        
        <!-- Interactive elements container -->
        <div class="elements-container" id="elementsContainer">
            <!-- Elements will be generated by JavaScript -->
        </div>
    </div>
    <script src="script.js"></script>
</body>
</html>
```

### Step 2: CSS Animations

#### A. Pulsing Animation (Sun Effect)
```css
.sun {
    width: 100px;
    height: 100px;
    background: radial-gradient(circle, #FFE135 0%, #FFD700 50%, #FFA500 100%);
    border-radius: 50%;
    box-shadow: 0 0 60px #FFD700;
    animation: sunPulse 4s ease-in-out infinite;
}

@keyframes sunPulse {
    0%, 100% { 
        transform: scale(1); 
        box-shadow: 0 0 60px #FFD700; 
    }
    50% { 
        transform: scale(1.05); 
        box-shadow: 0 0 80px #FFD700; 
    }
}
```

#### B. Floating Animation (Clouds)
```css
.cloud {
    position: absolute;
    animation: cloudFloat 20s linear infinite;
}

@keyframes cloudFloat {
    0% { transform: translateX(0); }
    100% { transform: translateX(-150vw); }
}
```

#### C. Bouncing Animation (Text)
```css
.title {
    animation: titleBounce 2s ease-in-out infinite;
}

@keyframes titleBounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}
```

#### D. Flutter Animation (Butterfly Wings)
```css
.butterfly svg {
    animation: flutter 0.2s ease-in-out infinite alternate;
}

@keyframes flutter {
    0% { transform: scaleX(1); }
    100% { transform: scaleX(0.7); }
}
```

### Step 3: Creating SVG Graphics Dynamically

#### SVG Template Function
```javascript
// Template function for creating SVG elements
const createSVGElement = (color, size = 1) => {
    return `
        <svg width="${80 * size}" height="${120 * size}" viewBox="0 0 80 120">
            <!-- SVG content here -->
            <circle cx="40" cy="40" r="30" fill="${color}" stroke="#333" stroke-width="2"/>
        </svg>
    `;
};

// Usage
const element = document.createElement('div');
element.innerHTML = createSVGElement('#FF69B4', 1.5);
document.body.appendChild(element);
```

#### Complex SVG Example (Flower)
```javascript
const flowerTemplate = (petalColor, size = 1) => {
    return `
        <svg width="${80 * size}" height="${120 * size}" viewBox="0 0 80 120">
            <!-- Stem -->
            <path d="M40 120 Q35 90 40 60" stroke="#228B22" stroke-width="4" fill="none"/>
            
            <!-- Petals arranged in a circle -->
            <g transform="translate(40, 35)">
                ${[0, 45, 90, 135, 180, 225, 270, 315].map(angle => `
                    <ellipse cx="0" cy="-18" rx="8" ry="18" 
                        fill="${petalColor}" 
                        transform="rotate(${angle})"/>
                `).join('')}
            </g>
            
            <!-- Center -->
            <circle cx="40" cy="35" r="12" fill="#FFD700"/>
        </svg>
    `;
};
```

### Step 4: GSAP Animations

#### A. Entrance Animation
```javascript
// Animate element appearing from above
gsap.from(element, {
    y: -200,           // Start 200px above
    opacity: 0,        // Start invisible
    scale: 0.5,        // Start at half size
    duration: 0.8,     // Animation duration
    delay: 0.2,        // Delay before starting
    ease: 'back.out(1.7)'  // Easing function
});
```

#### B. Continuous Swaying Animation
```javascript
function startSwaying(element) {
    const swayAmount = 5;  // Degrees of rotation
    const duration = 2;    // Seconds per sway
    
    gsap.to(element, {
        rotation: swayAmount,
        duration: duration,
        ease: 'sine.inOut',
        repeat: -1,        // Infinite repeat
        yoyo: true         // Reverse animation
    });
}
```

#### C. Elastic Bounce Effect
```javascript
// Bouncy scale effect
gsap.to(element, {
    scale: 1.1,
    duration: 0.5,
    ease: 'elastic.out(1, 0.5)'
});
```

### Step 5: Drag and Drop with GSAP Draggable

```javascript
// Make element draggable
Draggable.create(element, {
    type: 'x,y',           // Allow horizontal and vertical movement
    bounds: '.container',  // Constrain to container
    inertia: true,         // Physics-based momentum
    
    onDragStart: function() {
        // Actions when drag starts
        element.classList.add('dragging');
        gsap.to(element, { scale: 1.1, duration: 0.3 });
    },
    
    onDrag: function() {
        // Actions during drag
        console.log('Dragging at:', this.x, this.y);
    },
    
    onDragEnd: function() {
        // Actions when drag ends
        element.classList.remove('dragging');
        gsap.to(element, { 
            scale: 1, 
            duration: 0.5, 
            ease: 'elastic.out(1, 0.5)' 
        });
    }
});
```

### Step 6: Particle Effects (Sparkles)

```javascript
function createSparkles(x, y) {
    const container = document.getElementById('elementsContainer');
    
    for (let i = 0; i < 5; i++) {
        // Create sparkle element
        const sparkle = document.createElement('div');
        sparkle.className = 'sparkle';
        sparkle.style.left = `${x + (Math.random() - 0.5) * 50}px`;
        sparkle.style.top = `${y + (Math.random() - 0.5) * 50}px`;
        container.appendChild(sparkle);
        
        // Remove after animation completes
        setTimeout(() => sparkle.remove(), 1000);
    }
}
```

```css
.sparkle {
    position: absolute;
    width: 10px;
    height: 10px;
    background: radial-gradient(circle, white 0%, transparent 70%);
    border-radius: 50%;
    animation: sparkleAnim 1s ease-out forwards;
}

@keyframes sparkleAnim {
    0% { transform: scale(0); opacity: 1; }
    100% { transform: scale(2); opacity: 0; }
}
```

### Step 7: Ambient Effects

#### Wind Effect
```javascript
function windEffect() {
    const windStrength = 5 + Math.random() * 10;
    
    elements.forEach(element => {
        gsap.to(element, {
            rotation: `+=${windStrength * (Math.random() > 0.5 ? 1 : -1)}`,
            duration: 0.5,
            ease: 'power2.out',
            onComplete: () => startSwaying(element)
        });
    });
}

// Trigger wind periodically
setInterval(windEffect, 15000);
```

#### Flying Objects (Butterflies)
```javascript
function createButterfly() {
    const butterfly = document.createElement('div');
    butterfly.className = 'butterfly';
    butterfly.innerHTML = `<svg>...</svg>`;
    
    // Start position (off-screen left)
    butterfly.style.left = '-50px';
    butterfly.style.top = `${100 + Math.random() * 300}px`;
    
    container.appendChild(butterfly);
    
    // Fly across screen
    gsap.to(butterfly, {
        x: window.innerWidth + 100,
        duration: 10,
        ease: 'none',
        onComplete: () => butterfly.remove()
    });
    
    // Flutter up and down
    gsap.to(butterfly, {
        y: '+=30',
        duration: 1,
        ease: 'sine.inOut',
        repeat: -1,
        yoyo: true
    });
}

// Create butterflies periodically
setInterval(createButterfly, 5000);
```

---

## Code Examples

### Complete Animation Chain
```javascript
// Chain multiple animations
const tl = gsap.timeline();

tl.from('.title', { opacity: 0, y: -50, duration: 0.5 })
  .from('.subtitle', { opacity: 0, y: -30, duration: 0.5 }, '-=0.3')
  .from('.element', { scale: 0, duration: 0.8, ease: 'back.out(1.7)' }, '-=0.2');
```

### Color Utility Function
```javascript
// Darken or lighten a hex color
function shadeColor(color, percent) {
    const num = parseInt(color.replace('#', ''), 16);
    const amt = Math.round(2.55 * percent);
    const R = (num >> 16) + amt;
    const G = (num >> 8 & 0x00FF) + amt;
    const B = (num & 0x0000FF) + amt;
    
    return '#' + (0x1000000 + 
        (R < 255 ? R < 1 ? 0 : R : 255) * 0x10000 + 
        (G < 255 ? G < 1 ? 0 : G : 255) * 0x100 + 
        (B < 255 ? B < 1 ? 0 : B : 255)
    ).toString(16).slice(1);
}

// Usage
const darkerColor = shadeColor('#FF69B4', -20);  // Darker
const lighterColor = shadeColor('#FF69B4', 20);  // Lighter
```

---

## Tips & Best Practices

### Performance
1. **Use `transform` and `opacity`** - These properties are GPU-accelerated
2. **Avoid animating `width`, `height`, `margin`** - Causes layout reflows
3. **Use `will-change` sparingly** - Only on elements that will animate
4. **Clean up elements** - Remove elements after animations complete

### CSS
```css
.animated-element {
    will-change: transform, opacity;
    transform: translateZ(0);  /* Force GPU acceleration */
}
```

### JavaScript
```javascript
// Good: Using transform
gsap.to(element, { x: 100, rotation: 45, duration: 1 });

// Avoid: Using position properties
gsap.to(element, { left: '100px', duration: 1 });  // Causes reflow
```

### Accessibility
1. **Respect `prefers-reduced-motion`**
```css
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
    }
}
```

2. **Provide alternatives for interactive elements**

### Browser Compatibility
- GSAP works in all modern browsers
- CSS animations have excellent support
- Test on mobile devices for touch interactions

---

---

## Versión en Español

---

## Tabla de Contenidos
1. [Requisitos](#requisitos)
2. [Conceptos Fundamentales](#conceptos-fundamentales)
3. [Tutorial Paso a Paso](#tutorial-paso-a-paso)
4. [Ejemplos de Código](#ejemplos-de-código)
5. [Consejos y Mejores Prácticas](#consejos-y-mejores-prácticas)

---

## Requisitos

### Software y Librerías

| Requisito | Versión | Propósito |
|-----------|---------|-----------|
| Navegador Web Moderno | Chrome 80+, Firefox 75+, Safari 13+, Edge 80+ | Soporte CSS3 y ES6+ |
| GSAP (GreenSock Animation Platform) | 3.12.5+ | Librería de animación JavaScript |
| GSAP Draggable Plugin | 3.12.5+ | Funcionalidad de arrastrar y soltar |

### Enlaces CDN
```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/Draggable.min.js"></script>
```

### No Se Requieren Herramientas de Compilación
¡Este proyecto usa HTML, CSS y JavaScript puros! No necesitas npm, webpack ni empaquetadores.

---

## Conceptos Fundamentales

### 1. Animaciones CSS con Keyframes
Las animaciones CSS permiten que los elementos transiten suavemente entre estilos.

**Propiedades Clave:**
- `@keyframes` - Define la secuencia de animación
- `animation` - Aplica la animación keyframe
- `animation-duration` - Duración de la animación
- `animation-iteration-count` - Número de repeticiones (`infinite` para bucles)
- `animation-timing-function` - Curva de aceleración (ease, linear, ease-in-out)

### 2. SVG (Gráficos Vectoriales Escalables)
Los SVG son gráficos independientes de resolución definidos en formato XML.

**Ventajas:**
- Escalables sin pérdida de calidad
- Manipulables mediante JavaScript
- Estilizables con CSS
- Tamaños de archivo pequeños para gráficos simples

### 3. GSAP (GreenSock Animation Platform)
GSAP es una potente librería de animación JavaScript.

**Métodos Principales:**
- `gsap.to()` - Animar hacia valores específicos
- `gsap.from()` - Animar desde valores específicos
- `gsap.to(elemento, {duration, propiedad, ease})`
- Funciones de aceleración: `back.out`, `elastic.out`, `sine.inOut`, `power2.out`

### 4. GSAP Draggable
Permite funcionalidad de arrastrar y soltar con interacciones basadas en física.

**Características Principales:**
- Inercia (movimiento basado en momento)
- Restricciones de límites
- Callbacks (onDragStart, onDrag, onDragEnd)

---

## Tutorial Paso a Paso

### Paso 1: Estructura HTML

Crea un diseño basado en contenedores:

```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tu Proyecto de Animación</title>
    <link rel="stylesheet" href="styles.css">
    <!-- Librerías GSAP -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/Draggable.min.js"></script>
</head>
<body>
    <div class="contenedor">
        <!-- Elementos de fondo -->
        <div class="fondo">
            <div class="sol"></div>
            <div class="nube nube-1"></div>
        </div>
        
        <!-- Contenedor de elementos interactivos -->
        <div class="contenedor-elementos" id="contenedorElementos">
            <!-- Los elementos serán generados por JavaScript -->
        </div>
    </div>
    <script src="script.js"></script>
</body>
</html>
```

### Paso 2: Animaciones CSS

#### A. Animación de Pulso (Efecto Sol)
```css
.sol {
    width: 100px;
    height: 100px;
    background: radial-gradient(circle, #FFE135 0%, #FFD700 50%, #FFA500 100%);
    border-radius: 50%;
    box-shadow: 0 0 60px #FFD700;
    animation: pulsoSol 4s ease-in-out infinite;
}

@keyframes pulsoSol {
    0%, 100% { 
        transform: scale(1); 
        box-shadow: 0 0 60px #FFD700; 
    }
    50% { 
        transform: scale(1.05); 
        box-shadow: 0 0 80px #FFD700; 
    }
}
```

#### B. Animación Flotante (Nubes)
```css
.nube {
    position: absolute;
    animation: flotarNube 20s linear infinite;
}

@keyframes flotarNube {
    0% { transform: translateX(0); }
    100% { transform: translateX(-150vw); }
}
```

#### C. Animación de Rebote (Texto)
```css
.titulo {
    animation: reboteTitulo 2s ease-in-out infinite;
}

@keyframes reboteTitulo {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}
```

#### D. Animación de Aleteo (Alas de Mariposa)
```css
.mariposa svg {
    animation: aletear 0.2s ease-in-out infinite alternate;
}

@keyframes aletear {
    0% { transform: scaleX(1); }
    100% { transform: scaleX(0.7); }
}
```

### Paso 3: Crear Gráficos SVG Dinámicamente

#### Función de Plantilla SVG
```javascript
// Función plantilla para crear elementos SVG
const crearElementoSVG = (color, tamano = 1) => {
    return `
        <svg width="${80 * tamano}" height="${120 * tamano}" viewBox="0 0 80 120">
            <!-- Contenido SVG aquí -->
            <circle cx="40" cy="40" r="30" fill="${color}" stroke="#333" stroke-width="2"/>
        </svg>
    `;
};

// Uso
const elemento = document.createElement('div');
elemento.innerHTML = crearElementoSVG('#FF69B4', 1.5);
document.body.appendChild(elemento);
```

#### Ejemplo SVG Complejo (Flor)
```javascript
const plantillaFlor = (colorPetalos, tamano = 1) => {
    return `
        <svg width="${80 * tamano}" height="${120 * tamano}" viewBox="0 0 80 120">
            <!-- Tallo -->
            <path d="M40 120 Q35 90 40 60" stroke="#228B22" stroke-width="4" fill="none"/>
            
            <!-- Pétalos dispuestos en círculo -->
            <g transform="translate(40, 35)">
                ${[0, 45, 90, 135, 180, 225, 270, 315].map(angulo => `
                    <ellipse cx="0" cy="-18" rx="8" ry="18" 
                        fill="${colorPetalos}" 
                        transform="rotate(${angulo})"/>
                `).join('')}
            </g>
            
            <!-- Centro -->
            <circle cx="40" cy="35" r="12" fill="#FFD700"/>
        </svg>
    `;
};
```

### Paso 4: Animaciones GSAP

#### A. Animación de Entrada
```javascript
// Animar elemento apareciendo desde arriba
gsap.from(elemento, {
    y: -200,           // Comenzar 200px arriba
    opacity: 0,        // Comenzar invisible
    scale: 0.5,        // Comenzar a mitad de tamaño
    duration: 0.8,     // Duración de la animación
    delay: 0.2,        // Retraso antes de comenzar
    ease: 'back.out(1.7)'  // Función de aceleración
});
```

#### B. Animación de Balanceo Continuo
```javascript
function iniciarBalanceo(elemento) {
    const cantidadBalanceo = 5;  // Grados de rotación
    const duracion = 2;          // Segundos por balanceo
    
    gsap.to(elemento, {
        rotation: cantidadBalanceo,
        duration: duracion,
        ease: 'sine.inOut',
        repeat: -1,        // Repetir infinitamente
        yoyo: true         // Invertir animación
    });
}
```

#### C. Efecto de Rebote Elástico
```javascript
// Efecto de escala con rebote
gsap.to(elemento, {
    scale: 1.1,
    duration: 0.5,
    ease: 'elastic.out(1, 0.5)'
});
```

### Paso 5: Arrastrar y Soltar con GSAP Draggable

```javascript
// Hacer elemento arrastrable
Draggable.create(elemento, {
    type: 'x,y',           // Permitir movimiento horizontal y vertical
    bounds: '.contenedor', // Restringir al contenedor
    inertia: true,         // Momento basado en física
    
    onDragStart: function() {
        // Acciones al comenzar a arrastrar
        elemento.classList.add('arrastrando');
        gsap.to(elemento, { scale: 1.1, duration: 0.3 });
    },
    
    onDrag: function() {
        // Acciones durante el arrastre
        console.log('Arrastrando en:', this.x, this.y);
    },
    
    onDragEnd: function() {
        // Acciones al terminar el arrastre
        elemento.classList.remove('arrastrando');
        gsap.to(elemento, { 
            scale: 1, 
            duration: 0.5, 
            ease: 'elastic.out(1, 0.5)' 
        });
    }
});
```

### Paso 6: Efectos de Partículas (Destellos)

```javascript
function crearDestellos(x, y) {
    const contenedor = document.getElementById('contenedorElementos');
    
    for (let i = 0; i < 5; i++) {
        // Crear elemento destello
        const destello = document.createElement('div');
        destello.className = 'destello';
        destello.style.left = `${x + (Math.random() - 0.5) * 50}px`;
        destello.style.top = `${y + (Math.random() - 0.5) * 50}px`;
        contenedor.appendChild(destello);
        
        // Eliminar después de completar la animación
        setTimeout(() => destello.remove(), 1000);
    }
}
```

```css
.destello {
    position: absolute;
    width: 10px;
    height: 10px;
    background: radial-gradient(circle, white 0%, transparent 70%);
    border-radius: 50%;
    animation: animDestello 1s ease-out forwards;
}

@keyframes animDestello {
    0% { transform: scale(0); opacity: 1; }
    100% { transform: scale(2); opacity: 0; }
}
```

### Paso 7: Efectos Ambientales

#### Efecto de Viento
```javascript
function efectoViento() {
    const fuerzaViento = 5 + Math.random() * 10;
    
    elementos.forEach(elemento => {
        gsap.to(elemento, {
            rotation: `+=${fuerzaViento * (Math.random() > 0.5 ? 1 : -1)}`,
            duration: 0.5,
            ease: 'power2.out',
            onComplete: () => iniciarBalanceo(elemento)
        });
    });
}

// Activar viento periódicamente
setInterval(efectoViento, 15000);
```

#### Objetos Voladores (Mariposas)
```javascript
function crearMariposa() {
    const mariposa = document.createElement('div');
    mariposa.className = 'mariposa';
    mariposa.innerHTML = `<svg>...</svg>`;
    
    // Posición inicial (fuera de pantalla a la izquierda)
    mariposa.style.left = '-50px';
    mariposa.style.top = `${100 + Math.random() * 300}px`;
    
    contenedor.appendChild(mariposa);
    
    // Volar a través de la pantalla
    gsap.to(mariposa, {
        x: window.innerWidth + 100,
        duration: 10,
        ease: 'none',
        onComplete: () => mariposa.remove()
    });
    
    // Aletear arriba y abajo
    gsap.to(mariposa, {
        y: '+=30',
        duration: 1,
        ease: 'sine.inOut',
        repeat: -1,
        yoyo: true
    });
}

// Crear mariposas periódicamente
setInterval(crearMariposa, 5000);
```

---

## Ejemplos de Código

### Cadena de Animación Completa
```javascript
// Encadenar múltiples animaciones
const tl = gsap.timeline();

tl.from('.titulo', { opacity: 0, y: -50, duration: 0.5 })
  .from('.subtitulo', { opacity: 0, y: -30, duration: 0.5 }, '-=0.3')
  .from('.elemento', { scale: 0, duration: 0.8, ease: 'back.out(1.7)' }, '-=0.2');
```

### Función Utilidad de Color
```javascript
// Oscurecer o aclarar un color hexadecimal
function sombrearColor(color, porcentaje) {
    const num = parseInt(color.replace('#', ''), 16);
    const amt = Math.round(2.55 * porcentaje);
    const R = (num >> 16) + amt;
    const G = (num >> 8 & 0x00FF) + amt;
    const B = (num & 0x0000FF) + amt;
    
    return '#' + (0x1000000 + 
        (R < 255 ? R < 1 ? 0 : R : 255) * 0x10000 + 
        (G < 255 ? G < 1 ? 0 : G : 255) * 0x100 + 
        (B < 255 ? B < 1 ? 0 : B : 255)
    ).toString(16).slice(1);
}

// Uso
const colorOscuro = sombrearColor('#FF69B4', -20);  // Más oscuro
const colorClaro = sombrearColor('#FF69B4', 20);    // Más claro
```

---

## Consejos y Mejores Prácticas

### Rendimiento
1. **Usa `transform` y `opacity`** - Estas propiedades están aceleradas por GPU
2. **Evita animar `width`, `height`, `margin`** - Causan reflujos de layout
3. **Usa `will-change` con moderación** - Solo en elementos que se animarán
4. **Limpia los elementos** - Elimina elementos después de completar animaciones

### CSS
```css
.elemento-animado {
    will-change: transform, opacity;
    transform: translateZ(0);  /* Forzar aceleración GPU */
}
```

### JavaScript
```javascript
// Bien: Usando transform
gsap.to(elemento, { x: 100, rotation: 45, duration: 1 });

// Evitar: Usando propiedades de posición
gsap.to(elemento, { left: '100px', duration: 1 });  // Causa reflujo
```

### Accesibilidad
1. **Respeta `prefers-reduced-motion`**
```css
@media (prefers-reduced-motion: reduce) {
    * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
    }
}
```

2. **Proporciona alternativas para elementos interactivos**

### Compatibilidad de Navegadores
- GSAP funciona en todos los navegadores modernos
- Las animaciones CSS tienen excelente soporte
- Prueba en dispositivos móviles para interacciones táctiles

---

## Quick Reference / Referencia Rápida

### GSAP Easing Functions / Funciones de Aceleración GSAP

| Easing | Description (EN) | Descripción (ES) |
|--------|------------------|------------------|
| `linear` | Constant speed | Velocidad constante |
| `ease.in` | Slow start | Inicio lento |
| `ease.out` | Slow end | Final lento |
| `ease.inOut` | Slow start and end | Inicio y final lentos |
| `back.out` | Overshoots then returns | Se pasa y regresa |
| `elastic.out` | Bouncy spring effect | Efecto de resorte |
| `sine.inOut` | Smooth S-curve | Curva S suave |
| `power2.out` | Quadratic ease out | Aceleración cuadrática |

### CSS Animation Properties / Propiedades de Animación CSS

| Property | Description (EN) | Descripción (ES) |
|----------|------------------|------------------|
| `animation-name` | Keyframe name | Nombre del keyframe |
| `animation-duration` | Length of animation | Duración de la animación |
| `animation-timing-function` | Speed curve | Curva de velocidad |
| `animation-delay` | Delay before start | Retraso antes de iniciar |
| `animation-iteration-count` | Number of repeats | Número de repeticiones |
| `animation-direction` | Normal, reverse, alternate | Normal, inversa, alternada |
| `animation-fill-mode` | Styles before/after | Estilos antes/después |

---

## Resources / Recursos

- [GSAP Documentation](https://greensock.com/docs/)
- [GSAP Docs en Español](https://greensock.com/docs/) (English site with examples)
- [MDN CSS Animations](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Animations)
- [MDN SVG Tutorial](https://developer.mozilla.org/en-US/docs/Web/SVG/Tutorial)
- [CSS-Tricks Animation Guide](https://css-tricks.com/almanac/properties/a/animation/)

---

*Created for the Flower Garden Interactive Demo*
*Creado para la Demo Interactiva del Jardín de Flores*